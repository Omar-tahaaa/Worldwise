# Fix Date Handling Error

- [/] Fix date formatting in `City.jsx` to handle invalid dates
- [ ] Fix date formatting in `CityItem.jsx` to handle invalid dates
- [ ] Ensure dates are stored as ISO strings in Firebase
- [ ] Test the fix in production build
